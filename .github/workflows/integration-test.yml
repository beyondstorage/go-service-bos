name: "Integration Test"

on: [push]

jobs:
  integration_test:
    name: "Integration Test"
    runs-on: ubuntu-latest

    strategy:
      matrix:
        go: [ "1.15", "1.16" ]

    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}

      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Test
        env:
          STORAGE_BOS_INTEGRATION_TEST: ${{ secrets.STORAGE_INTEGRATION_TEST }}
          STORAGE_BOS_CREDENTIAL: ${{ secrets.STORAGE_BOS_CREDENTIAL }}
          STORAGE_BOS_NAME: ${{ secrets.STORAGE_BOS_NAME }}
          STORAGE_BOS_ENDPOINT: ${{ secrets.STORAGE_BOS_ENDPOINT }}
        run: make integration_test